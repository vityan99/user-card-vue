export interface IPhotos {
  description: string;
  id: number;
  title: string;
  url: string;
  user: number;
}

export interface IUser {
  id: number;
  name: string;
  role: string;
  img: string;
}
